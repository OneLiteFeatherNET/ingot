# Screenshots for pull request #13

This branch exists only to host the images embedded in
https://github.com/OneLiteFeatherNET/ingot/pull/13. It shares no history with `main`, is
never merged, and holds no source.

Deleting it breaks the images in that pull request.

| File | Shows |
|---|---|
| `header.png` | The tokens view at 1440px, before and after the header moved |
| `mobile.png` | The top of the overview at 390px, before and after |

Taken against `fake-api/fake-ingot-backend.js`, so the values are stub data.
