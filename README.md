# Screenshots for pull request #11

This branch exists only to host the images embedded in
https://github.com/OneLiteFeatherNET/ingot/pull/11. It shares no history with `main`, is
never merged, and holds no source.

Deleting it breaks the images in that pull request, so it stays for as long as the
discussion is worth reading back.

| File | Shows |
|---|---|
| `console.png` | The console with no connection, before and after |
| `login.png` | The login dialog with rejected credentials, before and after |
| `upload.png` | The upload target inside a repository, before and after |

All of them were taken against `fake-api/fake-ingot-backend.js`, so the values are stub
data and not a real instance.
