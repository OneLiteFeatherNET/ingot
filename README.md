# Screenshots for pull request #10

This branch exists only to host the images embedded in
https://github.com/OneLiteFeatherNET/ingot/pull/10. It shares no history with `main`, is
never merged, and holds no source.

Deleting it breaks the images in that pull request, so it stays for as long as the
discussion is worth reading back.

| File | Shows |
|---|---|
| `dashboard-desktop.png` | The dashboard before and after, light theme, 1440px |
| `dashboard-dark.png` | The rebuilt dashboard in the dark theme |
| `dashboard-mobile.png` | The rebuilt dashboard at 390px |
| `tabbar-mobile.png` | The tab bar at 390px, before and after |

All of them were taken against `fake-api/fake-ingot-backend.js`, so the values are stub
data and not a real instance.
